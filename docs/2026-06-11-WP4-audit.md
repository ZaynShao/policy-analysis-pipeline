# WP-4 审计结论（Claude，2026-06-11）

> 对象：WP-4a 夜间脚本收编（`aab069d`）+ WP-4c analysis_context 兼容 canonical（`7db6d78`）+ WP-4b 服务器部署两跑（report `2026-06-11-codex-wp4b-report.md`）。
> 审计方法：独立复跑 pytest + 红 commit 真红验证 + 服务器 state/summary 直读 + 数字闭合。

## 1. 过关审计：通过

- **WP-4a**：两脚本 `bash -n` 独立通过；contexts 脚本 rm -rf 严格限定三个 nightly 目录且容器内执行；signals 脚本与 WP-3b 临时脚本逻辑逐字等价（锁超时→跳过通知、链失败→失败通知）；runbook 升级"脚本=真相源"。585 passed。
- **WP-4c**：红 commit 真红（2 failed 独立复现）；旧形态回归测试保证零行为漂移；588 passed 独立复现；diff 仅 2 文件。
- **WP-4b（两跑）**：首跑监督门拦截真问题后干净停；续跑服务器直读全吻合——
  - `rel_vocabulary_seen` 8 种共 **1669** == canonical 总数（39% 边丢失问题数字级确认修复）；
  - signal_context：186+6 accepted → policy/theme/region context **94/13/6**；
  - analysis_context **582** 行（546 关系上下文 / 94 信号上下文 / 58 双有）；
  - ③-E inventory：1669 行、`endpoint_missing_count=0`、分布与我此前独立统计逐项一致；
  - vault 前后 HEAD 均 `307f8a4a`、status 干净（上下文链只写 /state，零 vault 写入红线达成）；
  - 03:00 cron 已切 repo 脚本、临时脚本已清理（WP-3 配置漂移闭账）；03:30 cron 装好；crontab 共 12 条。

## 2. 阶段评估

- **④备料层 + ③-E 上云达成**：夜间线现为 02:00 关系增量 → 03:00 信号链 → 03:30 上下文链，同一把锁串行，各自独立失败告警。roadmap WP-4 验收门（"夜间链一次跑通，产物时间戳 VPS 当日"）手跑已过，**明晨 6/12 三链首次无人值守自跑是终验**。
- 监督门价值再次兑现：6/4 模块与 6/6 canonical 的输入契约断层（650 边/39% 落词表外）被部署前拦截，未进 cron。
- 新基线：analysis_context 582 / policy_context 94 / inventory 1669。

## 3. 洞察

1. **"设计期模块消费正式产物前必须做契约对账"**：analysis_context 是 6/4 设计、canonical 6/6 定型，中间无人回头对账。WP-5 summaries 重建吸取：生产者直接以 SCHEMA 契约+当前产物实测形态为输入定义，不引用设计期中间格式。
2. 未知 rel 从 raise 改为可见计数（`other_rel_counts`+`rel_vocabulary_seen`）——夜间链对词表演化免疫，且演化可观测。该模式值得在其他严格词表消费者上复用（排 WP-6 顺手盘点）。
3. analysis_context 582/1224 政策有上下文：覆盖率 48%，主要受关系/信号联通度限制，属数据现实而非缺陷；L3 规范设计时应知道这个分母。

## 结论：WP-4 关闭，WP-5 放行。
