# WP-2 审计结论（Claude，2026-06-11）

> 对象：WP-2a 增量编排器代码（`11424f1` 等，已审合 main）+ WP-2b 服务器部署与 361 篇补课（report `2026-06-11-codex-wp2b-report.md` 三节：首跑/续跑/续跑2）。
> 审计方法：不采信 Codex 自述——服务器实态独立直读、数字闭合性核算、commit 纯净性核查。

## 1. 过关审计：通过

服务器实态独立直读（2026-06-11 16:1x CST）全部吻合：

- `last_sync_run.json`：relation_count **1649**（基线 1123，+526）/ errors=[] / synced 1176 / invalid 15 / commentary 171。
- vault HEAD `69eb62e4` == origin/main，作者 policy-pipeline-vps，message 与 handoff 逐字一致；改动 551 文件**全部**在 `1_extracted/relations/` 白名单内（逐路径核查零越界）；工作树干净。
- canonical 1669 行；状态文件 covered=1223 / sem 累积 668 / judged ledger 431。
- crontab：`relations_increment` 行恰 1 条，带 flock -w 7200 + 锁超时 notify + produce_and_push 白名单 + 失败 notify + relations.log，与 runbook §1 一致。
- 监督门复核：new_pids=**361**（与 WP-0 补课集合分毫不差）；judged=431（估 407，+5.9%，门内）；canonical 1669 ≥ 1138 只增不减；投影 1649 > 1123。
- 报告 commit `c63fae4` 仅含报告文件本身（206 行插入）。

**数字闭合**：HPR **1019** = WP-0 全量 ③-B 扫描候选数（同语料确定性复现）；SRC **650** = 668 累积 − 18 与 HPR 重叠去重；悬挂 20 = 1669 − 1649（端点含 invalid 15）；covered 1223 = 873 + 361 − 11（dup-pid wart 既知，raw 1224 文件/唯一 pid 1213）。

**过程三次中止均按红线干净停**（每次 vault 复核干净、cron 未半装）：
1. vault 浅克隆缺 `a6fb3c09` 历史 → Codex 自愈 `git fetch --deepen=100`，covered=873 门内；
2. 镜像缺 git（`python:3.12-slim`）→ Dockerfile 单行修复（`9f78512`，Claude 审计合 main）；
3. pip 假性 ResolutionImpossible（`/simple/httpcore/` ReadTimeout 致解析器视 httpcore 无候选，非真依赖冲突）→ 按 Claude 判读重试一次即过。

## 2. 阶段评估

- **③关系族云上闭环达成**：生产闭环（02:00 cron：增量 judge → 防塌缩 apply 闸 → produce_and_push）+ 覆盖闭环（361 篇补课全部入账，pid ledger 推进到 1223）+ 可见闭环（失败/锁超时双 notify）。
- 新基线：**投影 relation 1649 / canonical 1669**。WP-3 起以此为准。
- 判定接受率 30.4%（131/431）低于种子期 ~53%：方向合理（增量候选=新×存量，先验本来更低），不阻塞；留作 L2 质量抽样的一个视角。
- 残留观察点：**明晨 02:00 首次 cron 自跑**（cron 环境 vs 手跑差异：env 加载、docker compose 路径、日志落盘），列入 WP-3 期间观察。

## 3. 洞察

1. **构建可重复性=层缓存运气**：pyproject 依赖全无上界（`anthropic>=0.40` 等），今日缓存一失效就连环触发两次构建失败。排 WP-6：constraints/锁文件 + 不依赖缓存的干净重建演练。
2. **长任务无心跳**：dry-run 27 分钟零输出，监督只能靠 judge_calls.jsonl 探针。建议 run_increment 每 50 对打一行进度到 stdout（小改，排 WP-6 前顺手做）。
3. ledger covered 含 ~10 个已删 pid（6/10 清理）与 11 dup-pid：对"勿重判"语义无害，记录不修。
4. 补课型大批次（数百对）串行 judge ~30 分钟可接受；日常增量（每日个位数 pid）成本忽略不计。不为此加并发。

## 结论：WP-2 关闭，WP-3 放行。
