# WP-3 审计结论（Claude，2026-06-11）

> 对象：WP-3a 信号线代码（branch `wp3/signals-nightly`，合 main `c638cef`）+ WP-3b 服务器部署（report `2026-06-11-codex-wp3b-report.md` 两节）。
> 审计方法：独立复跑 pytest + 红 commit 真红验证 + 服务器实态直读 + 数字闭合核算。

## 1. 过关审计：通过

**WP-3a（代码）**：
- diff 5 文件全在契约内；TDD 红绿分 commit，且红 commit 真红独立验证（checkout `dc503a3` 跑新测试 2 failed——其中 `test_daily_check_relays_when_token_invalid_even_if_feed_valid` 即今晨 feed-200 误判 bug 的精确场景）。
- 独立复跑 `python3 -m pytest`：**585 passed**。
- 03:00 cron 行四段 flag 与三个模块 argparse 签名逐一吻合；判活决策点改 `token_checker(db_path)`（sqlite 直检），feed 降级诊断。

**WP-3b（部署+首跑）**，服务器直读全部吻合：
- vault HEAD `307f8a4a`（author policy-pipeline-vps，恰两文件且均在白名单，工作树干净 HEAD==origin）。
- `1_extracted/commentary_signals.jsonl` **186 行** / `market_intel_signals.jsonl` **6 行**，与 apply summary、投影 `commentary_count=186` 三方一致。
- 数字闭合：405 评论 → 211 联通发信号 + 25 review queue + 142 unlinked + 52 非政策相关；23 manifest → 19 located（4 进 `manifest_pid_not_found` queue）→ derived 闸后 6 发布；**全程 signal-or-queue，零静默丢失**。
- `relation_count=1649` 不动（信号链未触关系），errors=[]。
- 03:00 cron 装好（1 条）；哨兵实测 `valid=True, relayed=false`，detail 以 token 口径为决策、feed 为诊断后缀。

**偏差（已定性，处置排 WP-4）**：runbook 03:00 超长行被 crontab 拒绝（`command too long`），Codex 落地为 `/root/policy-pipeline-state/bin/commentary_signals.run_nightly.sh` 包装脚本 + 短 cron 行。脚本内容逐字核对=runbook 逻辑（flock/四段/白名单/notify）。**问题：脚本不在 git，runbook 仍是长行形态，构成配置漂移**。处置：WP-4 把脚本收编进仓库（`scripts/service/` 下），cron 指向 checkout 内路径，runbook 改短行+脚本引用；WP-4 自己的链直接用同模式。

## 2. 阶段评估

- **评论校准信号闭环达成**：生产（03:00 cron 全链）+ 覆盖（每晚 405+ 评论全量确定性重扫，随 07:30 ingest 自然增长）+ 可见（失败/锁超时 notify）。投影 171→**186**。
- market_intel 按用户裁决（roadmap 决策表 #3）冻结为存量再生产：23 行 manifest → 每晚 6 条信号，无增长线、无登记器。**WP-3 范围就此关闭，无 WP-3c**。
- 新基线：commentary_count **186**（每日浮动属预期，随评论 ingest 增长）；signals 文件 186/6。
- 观察点（并入明晨检查）：6/12 凌晨 02:00 关系增量 + 03:00 信号链两条 cron 首次自跑，查 relations.log / signals.log / vault 两个机器 commit。

## 3. 洞察

1. **"runbook 原样照抄"契约碰到 crontab 行长上限**——长链 cron 的正确形态是 repo 内脚本 + 短行调用。WP-4 统一收编（含已落的 03:00 脚本），runbook 契约从"行原文"升级为"脚本文件+短行"。
2. **评论联通率 52%**（211/405，142 条 unlinked 不发信号）：设计内行为（无 related_policy_ids 不发），但这个比例值得在 L2 质量抽样时看一眼 unlinked 大头是真不相关还是关联抽取漏。
3. **review queue 在积累无消费**（commentary 25 + market 18 + L1 队列）：人工池消费机制是跨族共性缺口，WP-6 的 `l1_review_consumer` 接 cron 时一并考虑信号队列的可见性（哪怕只是巡检报数）。
4. 哨兵双口径（token 决策 + feed 诊断）实测形态良好，今晨级别的误判从机制上不可再现。

## 结论：WP-3 关闭，WP-4 放行。
