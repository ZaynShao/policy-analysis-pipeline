# L1 机制完善 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 L1 采集机制补一个统一 review pool(所有"机器拿不准"项的进池 IN)+ 核验门精度修 + 浅抓取健壮 + 文档对齐,杜绝静默丢弃;人交互面/回灌消费者显式交 B14。

**Architecture:** 新增 `review_pool.py`(只写不排的池写入器)。4 个生产者(gate 低置信 / discovery 候选 / sweep 命中 / fetch 失败)各自把项 append 进 `state/l1_review/pool.jsonl`。核验门 `_institution_match` 从 marker 单一信号改为 host 行政区(省/市拼音段)+ marker 双信号,减少真站误判候选。抓取失败重试一次再入池。回灌消费者**不建**(过渡用现有 oneshots)。

**Tech Stack:** Python 3.9,pytest(mock 网络,沿用 `tests/l1_collect/`),无新依赖。

**Spec:** `docs/superpowers/specs/2026-06-08-l1机制完善-design.md`

**纪律:** §C(LLM 判定不写 raw,池在 `state/` 派生层);pool 写入包 try/except 绝不中断渠道循环;按 (kind,ref) 去重;TDD;每 task 一 commit。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `scripts/l1_collect/review_pool.py` | 池写入器:append(去重)/load/summarize + verdict 契约常量 | Create |
| `scripts/l1_collect/run_incremental.py` | `_gate_extracted_dir` 拆 review_queue→池;fetch 失败→池;收尾 summary | Modify |
| `scripts/l1_collect/step4_fetch.py` | fetch 失败重试一次 | Modify |
| `scripts/l1_collect/channel_discovery.py` | `_institution_match` 改 host 行政区+marker 双信号 | Modify |
| `scripts/_oneshot/expand_channels_l1.py` | 发现产候选→池 | Modify |
| `scripts/_oneshot/sweep_existing_commentary.py` | 命中→池 | Modify |
| `tests/l1_collect/test_review_pool.py` | 池单测 | Create |
| `tests/l1_collect/test_gate_review_pool.py` | gate→池 路由测 | Create |
| `tests/l1_collect/test_institution_area.py` | 核验门 host 行政区测 | Create |
| `tests/l1_collect/test_fetch_retry.py` | 抓取重试测 | Create |
| `docs/策略-八步采集法.md`(vault 或 pipeline docs) | 重写为纯 L1 SOP | Modify |
| `docs/BACKLOG.md` | 追加 B15 + 核验门标 ✅ | Modify |

---

## Task 1: review_pool 池写入器(基础,无依赖)

**Files:**
- Create: `scripts/l1_collect/review_pool.py`
- Test: `tests/l1_collect/test_review_pool.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/l1_collect/test_review_pool.py
from pathlib import Path
from scripts.l1_collect import review_pool as rp


def _entry(kind="gate", ref="r1"):
    return {"kind": kind, "ref": ref, "reason": "low_conf", "suggested_action": "review",
            "confidence": 0.4, "evidence": "ev", "channel": "ch", "run_label": "run1"}


def test_append_writes_and_dedups(tmp_path):
    pool = tmp_path / "pool.jsonl"
    assert rp.append(_entry(), pool_path=pool) is True
    assert rp.append(_entry(), pool_path=pool) is False          # 同 (kind,ref) 去重
    assert rp.append(_entry(ref="r2"), pool_path=pool) is True
    rows = rp.load(pool_path=pool)
    assert len(rows) == 2
    assert {r["ref"] for r in rows} == {"r1", "r2"}


def test_append_different_kind_same_ref_not_deduped(tmp_path):
    pool = tmp_path / "pool.jsonl"
    rp.append(_entry(kind="gate", ref="x"), pool_path=pool)
    assert rp.append(_entry(kind="sweep", ref="x"), pool_path=pool) is True   # kind 不同→不去重
    assert len(rp.load(pool_path=pool)) == 2


def test_summarize_counts_by_kind(tmp_path):
    pool = tmp_path / "pool.jsonl"
    rp.append(_entry(kind="gate", ref="a"), pool_path=pool)
    rp.append(_entry(kind="fetch_fail", ref="b"), pool_path=pool)
    rp.append(_entry(kind="fetch_fail", ref="c"), pool_path=pool)
    s = rp.summarize(pool_path=pool)
    assert s == {"gate": 1, "fetch_fail": 2}


def test_load_missing_returns_empty(tmp_path):
    assert rp.load(pool_path=tmp_path / "nope.jsonl") == []
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/dev/政策分析-pipeline-l1-repair && python3 -m pytest tests/l1_collect/test_review_pool.py -q`
Expected: FAIL(`ModuleNotFoundError: review_pool`)

- [ ] **Step 3: 实现**

```python
# scripts/l1_collect/review_pool.py
"""L1 review pool:所有"机器拿不准"项的统一进池(IN)。只写不排——
人交互面 + 标准化回灌消费者交 B14。本模块只负责 append(去重)/load/summarize。

OUT 契约(文档化"将来正式回灌消费什么";本线不建消费者,过渡用现有 oneshots):
  记录格式 {ref, kind, verdict, corrections?, reviewer, note, decided_run}
"""
from __future__ import annotations
import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
POOL = ROOT / "state" / "l1_review" / "pool.jsonl"

# 每 kind 允许的人工裁决(供 B14 标准化 + 将来回灌消费者)
VERDICTS = {
    "gate": ("pass", "commentary", "reject"),
    "checkpoint": ("promote", "drop"),
    "sweep": ("confirm", "keep"),
    "fetch_fail": ("retry", "unfetchable", "drop"),
}

_KEYS = ("kind", "ref", "reason", "suggested_action", "confidence",
         "evidence", "channel", "run_label")


def load(pool_path: Path = POOL) -> list:
    if not pool_path.exists():
        return []
    out = []
    for line in pool_path.read_text(encoding="utf-8").split("\n"):
        if line.strip():
            out.append(json.loads(line))
    return out


def append(entry: dict, pool_path: Path = POOL) -> bool:
    """按 (kind, ref) 去重写入。已存在→跳过返回 False。"""
    key = (entry.get("kind"), entry.get("ref"))
    for r in load(pool_path):
        if (r.get("kind"), r.get("ref")) == key:
            return False
    row = {k: entry.get(k) for k in _KEYS}
    pool_path.parent.mkdir(parents=True, exist_ok=True)
    with open(pool_path, "a", encoding="utf-8") as f:
        f.write(json.dumps(row, ensure_ascii=False) + "\n")
    return True


def summarize(pool_path: Path = POOL) -> dict:
    out: dict = {}
    for r in load(pool_path):
        k = r.get("kind", "?")
        out[k] = out.get(k, 0) + 1
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m pytest tests/l1_collect/test_review_pool.py -q`
Expected: PASS(4 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/l1_collect/review_pool.py tests/l1_collect/test_review_pool.py
git commit -m "feat(l1): review_pool 池写入器(append去重/load/summarize + verdict契约)"
```

---

## Task 2: gate 低置信 → 池(拆出 review_queue,不再当 reject 丢)

**现状**(`run_incremental.py:77-80`):`_gate_extracted_dir` 的 `else` 把 reject 和 review_queue 一起塞 `gate_rejects.jsonl`、计 `n_rej`、ext 项不保留 → 低置信项被当 reject 丢、从不浮给人审。

**Files:**
- Modify: `scripts/l1_collect/run_incremental.py`(`_gate_extracted_dir` + `_run_channel` + summary)
- Test: `tests/l1_collect/test_gate_review_pool.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/l1_collect/test_gate_review_pool.py
import json
from pathlib import Path
from scripts.l1_collect import run_incremental as ri
from scripts.l1_collect import review_pool as rp


def _write_ext(ext_dir: Path, name: str, title: str):
    ext_dir.mkdir(parents=True, exist_ok=True)
    (ext_dir / f"{name}.json").write_text(json.dumps(
        {"url": "http://x.gov.cn/a", "title": title, "body": "正文"}, ensure_ascii=False),
        encoding="utf-8")


def test_review_queue_goes_to_pool_not_rejects(tmp_path):
    ext = tmp_path / "ext"; _write_ext(ext, "doc1", "某灰区标题")
    pool = tmp_path / "pool.jsonl"
    review_dir = tmp_path / "review"
    # llm 返回低置信非政策 → gate action=review_queue
    def llm(system, user):
        return json.dumps({"label": "non_policy_news", "confidence": 0.3, "evidence": "灰"})
    res = ri._gate_extracted_dir(
        ext, tmp_path / "passed", tmp_path / "comm",
        tmp_path / "quar" / "gate_rejects.jsonl", llm,
        pool_path=pool, review_dir=review_dir)
    n_pass, n_comm, n_rej, n_review = res
    assert (n_pass, n_comm, n_rej, n_review) == (0, 0, 0, 1)
    rows = rp.load(pool_path=pool)
    assert len(rows) == 1 and rows[0]["kind"] == "gate" and rows[0]["ref"] == "doc1"
    assert (review_dir / "doc1.json").exists()           # ext 项保留供回灌
    # gate_rejects.jsonl 不含该项
    rej = tmp_path / "quar" / "gate_rejects.jsonl"
    assert not rej.exists() or rej.read_text(encoding="utf-8").strip() == ""


def test_real_reject_still_quarantined(tmp_path):
    ext = tmp_path / "ext"; _write_ext(ext, "doc2", "普通新闻")
    pool = tmp_path / "pool.jsonl"
    def llm(system, user):
        return json.dumps({"label": "non_policy_news", "confidence": 0.95, "evidence": "新闻"})
    res = ri._gate_extracted_dir(
        ext, tmp_path / "passed", tmp_path / "comm",
        tmp_path / "quar" / "gate_rejects.jsonl", llm,
        pool_path=pool, review_dir=tmp_path / "review")
    assert res == (0, 0, 1, 0)
    assert rp.load(pool_path=pool) == []
    assert (tmp_path / "quar" / "gate_rejects.jsonl").exists()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/l1_collect/test_gate_review_pool.py -q`
Expected: FAIL(`_gate_extracted_dir` 不接受 `pool_path`/`review_dir`,返回 3 元组)

- [ ] **Step 3: 改实现**

`run_incremental.py` 顶部加 import:
```python
from . import review_pool
```

`_gate_extracted_dir` 整体替换为(新增 `pool_path`/`review_dir` 形参 + review_queue 第 4 路):
```python
def _gate_extracted_dir(ext_dir: Path, passed_dir: Path, comm_dir: Path,
                        quar_jsonl: Path, llm_fn, *,
                        pool_path: Path = review_pool.POOL,
                        review_dir: Path = STATE / "T1_incremental" / "review",
                        run_label: str = "") -> tuple:
    passed_dir.mkdir(parents=True, exist_ok=True)
    comm_dir.mkdir(parents=True, exist_ok=True)
    n_pass = n_comm = n_rej = n_review = 0
    rejects: list = []
    for jf in sorted(ext_dir.glob("*.json")):
        rec = json.loads(jf.read_text(encoding="utf-8"))
        gr = gate_one(ref=jf.stem, url=rec.get("url", ""), title=rec.get("title", ""),
                      body_head=(rec.get("body") or "")[:800], llm_fn=llm_fn)
        if gr.action == "pass":
            (passed_dir / jf.name).write_text(jf.read_text(encoding="utf-8"), encoding="utf-8")
            n_pass += 1
        elif gr.action == "commentary":
            (comm_dir / jf.name).write_text(jf.read_text(encoding="utf-8"), encoding="utf-8")
            n_comm += 1
        elif gr.action == "review_queue":
            review_dir.mkdir(parents=True, exist_ok=True)
            (review_dir / jf.name).write_text(jf.read_text(encoding="utf-8"), encoding="utf-8")
            try:
                review_pool.append({
                    "kind": "gate", "ref": jf.stem, "reason": gr.evidence or "low_conf",
                    "suggested_action": "review", "confidence": gr.confidence,
                    "evidence": rec.get("title", "")[:60], "channel": run_label,
                    "run_label": run_label}, pool_path=pool_path)
            except Exception as e:
                print(f"  [pool-write 失败] gate/{jf.stem}: {str(e)[:80]}")
            n_review += 1
        else:
            rejects.append({"file": jf.name, "url": rec.get("url", ""),
                            "title": rec.get("title", ""), **gr.to_dict()})
            n_rej += 1
    if rejects:
        quar_jsonl.parent.mkdir(parents=True, exist_ok=True)
        with open(quar_jsonl, "a", encoding="utf-8") as f:
            for r in rejects:
                f.write(json.dumps(r, ensure_ascii=False) + "\n")
    return n_pass, n_comm, n_rej, n_review
```

`_run_channel` 改调用处(解包 4 元组 + 传 run_label;约 121-123 行):
```python
    n_pass, n_comm, n_rej, n_review = _gate_extracted_dir(
        sd / "ext", sd / "passed", sd / "comm_ext",
        sd / "quar" / "gate_rejects.jsonl", llm_fn, run_label=label)
```
并在 `_run_channel` 返回 dict 加 `"gate_review": n_review`(约 135-137 行):
```python
    return {"channel": label, "scanned": n_scan, "kept": kept,
            "gate_passed": n_pass, "gate_commentary": n_comm, "gate_rejected": n_rej,
            "gate_review": n_review,
            "ingested": ing_ok, "ingested_commentary": n_comm_ing}
```
`run_incremental` summary 加一行(约 157 行后):
```python
        "total_gate_review": sum(r.get("gate_review", 0) for r in results),
```

**注意**:`_run_channel` 清理循环(131 行)**不要**加 `review` —— review/ 项需保留供回灌。

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m pytest tests/l1_collect/test_gate_review_pool.py tests/l1_collect/test_run_incremental.py -q`
Expected: PASS(新 2 测试 + 旧 run_incremental 测试,若旧测试断言 3 元组需同步改为 4 元组)

- [ ] **Step 5: Commit**

```bash
git add scripts/l1_collect/run_incremental.py tests/l1_collect/test_gate_review_pool.py
git commit -m "fix(l1): gate review_queue 拆出第4路→review_pool(不再当reject丢·ext保留供回灌)"
```

---

## Task 3: fetch 失败 → 重试一次 → 池

**Files:**
- Modify: `scripts/l1_collect/step4_fetch.py`(失败重试一次)
- Modify: `scripts/l1_collect/run_incremental.py`(`_run_channel` 抓取后读 ferr→池)
- Test: `tests/l1_collect/test_fetch_retry.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/l1_collect/test_fetch_retry.py
import json
from pathlib import Path
import scripts.l1_collect.step4_fetch as s4


class _Res:
    def __init__(self, via, body=""):
        self.via = via; self.body = body


def test_fetch_retries_once_then_succeeds(tmp_path, monkeypatch):
    calls = {"n": 0}
    def fake_fetch(url):
        calls["n"] += 1
        return _Res("fetch_error") if calls["n"] == 1 else _Res("firecrawl", "正文够长")
    monkeypatch.setattr(s4, "fetch_article", fake_fetch)
    inp = tmp_path / "cand.jsonl"
    inp.write_text(json.dumps({"url": "http://x.gov.cn/a", "title": "t"}) + "\n", encoding="utf-8")
    success, error = s4.fetch_candidates(inp, tmp_path / "fetch", tmp_path / "ferr.txt")
    assert calls["n"] == 2 and success == 1 and error == 0


def test_fetch_still_fails_after_retry_logged(tmp_path, monkeypatch):
    monkeypatch.setattr(s4, "fetch_article", lambda url: _Res("fetch_error"))
    inp = tmp_path / "cand.jsonl"
    inp.write_text(json.dumps({"url": "http://x.gov.cn/a", "title": "t"}) + "\n", encoding="utf-8")
    success, error = s4.fetch_candidates(inp, tmp_path / "fetch", tmp_path / "ferr.txt")
    assert success == 0 and error == 1
    assert (tmp_path / "ferr.txt").read_text(encoding="utf-8").strip() == "http://x.gov.cn/a"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/l1_collect/test_fetch_retry.py -q`
Expected: FAIL(`test_fetch_retries_once` 期望 calls==2,实际 1)

- [ ] **Step 3: 改 step4_fetch.py**

在 `fetch_candidates` 循环里,把 `res = fetch_article(url)` 替换为带一次重试:
```python
        res = fetch_article(url)
        if res.via == "fetch_error":
            res = fetch_article(url)          # 浅重试一次(瞬时失败兜底)
```

- [ ] **Step 4: run_incremental `_run_channel` 抓取后读 ferr → 池**

在 `_run_channel` 里 `fetch_candidates(...)` 之后、`extract_all(...)` 之前插入:
```python
        ferr = sd / "quar" / f"{label}__ferr.txt"
        if ferr.exists():
            for u in ferr.read_text(encoding="utf-8").split("\n"):
                if not u.strip():
                    continue
                try:
                    review_pool.append({
                        "kind": "fetch_fail", "ref": u.strip(),
                        "reason": "fetch_error_after_retry", "suggested_action": "retry",
                        "confidence": None, "evidence": "", "channel": label,
                        "run_label": label})
                except Exception as e:
                    print(f"  [pool-write 失败] fetch_fail: {str(e)[:80]}")
```
(`review_pool` 已在 Task 2 import。)

- [ ] **Step 5: 跑测试确认通过**

Run: `python3 -m pytest tests/l1_collect/test_fetch_retry.py -q`
Expected: PASS(2 passed)

- [ ] **Step 6: Commit**

```bash
git add scripts/l1_collect/step4_fetch.py scripts/l1_collect/run_incremental.py tests/l1_collect/test_fetch_retry.py
git commit -m "feat(l1): 抓取失败重试一次+仍失败→fetch_fail入池(不再静默ferr)"
```

---

## Task 4: 核验门 host 行政区校验(减入池·堵真滑坡)

**现状**(`channel_discovery.py:111-117`):`_institution_match(domain, channel_type)` 仅 marker 单信号 → 真站(dgboc/zcom/sxdofcom…)漏判候选。

**改为**:host 行政区(省/市拼音段)**或** marker,二者任一命中即过。target 已知期望省份(`commerce_market_targets` 带 `province`),市级 target 加市拼音段。

**Files:**
- Modify: `scripts/l1_collect/channel_discovery.py`(加 `_PROV_TOKENS`/`_CITY_TOKENS`/`_area_match`;改 `_institution_match` 签名 + `discover_one:221` 调用)
- Test: `tests/l1_collect/test_institution_area.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/l1_collect/test_institution_area.py
from scripts.l1_collect.channel_discovery import _institution_match


def _t(prov, ctype, level="省", city_code=""):
    return {"province": prov, "channel_type": ctype, "level": level, "city_code": city_code}


def test_real_province_sites_pass_by_area():
    assert _institution_match("zcom.zj.gov.cn", _t("浙江省", "商务"))          # zj 省段
    assert _institution_match("sxdofcom.shaanxi.gov.cn", _t("陕西省", "市监"))  # shaanxi 省段
    assert _institution_match("commerce.ah.gov.cn", _t("安徽省", "商务"))       # ah 省段


def test_real_city_site_pass_by_city_token():
    # 东莞市商务局(加油线市级 441900)→ dg 市段
    assert _institution_match("dgboc.dg.gov.cn", _t("广东省", "商务", "市", "441900"))


def test_marker_still_passes_when_area_absent():
    assert _institution_match("swt.fujian.gov.cn", _t("福建省", "商务"))        # fujian 段+swt marker


def test_cross_institution_rejected():
    assert not _institution_match("hq.mof.gov.cn", _t("海南省", "商务"))         # 财政部·非海南段
    assert not _institution_match("conghua.gov.cn", _t("广东省", "商务"))        # 从化区·无 gd 段无 marker


def test_non_commerce_market_type_passes():
    assert _institution_match("anything.gov.cn", _t("浙江省", "发改委"))         # 非商务/市监→True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/l1_collect/test_institution_area.py -q`
Expected: FAIL(`_institution_match` 旧签名 `(domain, channel_type)`,传 dict 报错/逻辑不符)

- [ ] **Step 3: 改 channel_discovery.py**

在 `_INST_DOMAIN_MARKERS`(104 行)下面加行政区 token 表:
```python
# 省 → gov 域拼音段(缩写+全拼);host 按 "." 分段做相等匹配,避免子串误命中
_PROV_TOKENS = {
    "北京市": ("bj", "beijing"), "天津市": ("tj", "tianjin"), "河北省": ("he", "hebei"),
    "山西省": ("sx", "shanxi"), "内蒙古自治区": ("nm", "nmg", "neimenggu"),
    "辽宁省": ("ln", "liaoning"), "吉林省": ("jl", "jilin"), "黑龙江省": ("hlj", "heilongjiang"),
    "上海市": ("sh", "shanghai"), "江苏省": ("js", "jiangsu"), "浙江省": ("zj", "zhejiang"),
    "安徽省": ("ah", "anhui"), "福建省": ("fj", "fujian"), "江西省": ("jx", "jiangxi"),
    "山东省": ("sd", "shandong"), "河南省": ("ha", "henan"), "湖北省": ("hb", "hubei"),
    "湖南省": ("hn", "hunan"), "广东省": ("gd", "guangdong"), "广西壮族自治区": ("gx", "guangxi"),
    "海南省": ("hi", "hainan"), "重庆市": ("cq", "chongqing"), "四川省": ("sc", "sichuan"),
    "贵州省": ("gz", "guizhou"), "云南省": ("yn", "yunnan"), "西藏自治区": ("xz", "xizang", "tibet"),
    "陕西省": ("shaanxi", "sn"), "甘肃省": ("gs", "gansu"), "青海省": ("qh", "qinghai"),
    "宁夏回族自治区": ("nx", "ningxia"), "新疆维吾尔自治区": ("xj", "xinjiang"),
}
# 加油线 10 重点市国标码 → gov 域拼音段(缩写+全拼)
_CITY_TOKENS = {
    "441900": ("dg", "dongguan"), "440600": ("fs", "foshan"), "330400": ("jx", "jiaxing"),
    "330300": ("wz", "wenzhou"), "350500": ("qz", "quanzhou"), "320600": ("nt", "nantong"),
    "370600": ("yt", "yantai"), "370700": ("wf", "weifang"), "320400": ("cz", "changzhou"),
    "441300": ("hz", "huizhou"),
}


def _area_match(domain: str, target: dict) -> bool:
    """host 按 '.' 分段,任一段 == 期望省/市拼音 token 即命中。"""
    segs = (domain or "").lower().split(".")
    tokens = set(_PROV_TOKENS.get(target.get("province", ""), ()))
    if target.get("level") == "市":
        tokens |= set(_CITY_TOKENS.get(target.get("city_code", ""), ()))
    return any(t in segs for t in tokens)
```

把 `_institution_match`(111-117 行)整体替换(签名改为收 target dict):
```python
def _institution_match(domain: str, target: dict) -> bool:
    """域名无关发现路径核验:host 行政区段 或 机构 marker 命中即过。
    非商务/市监 channel_type 返回 True(发改委/能源等不受影响)。"""
    ctype = target.get("channel_type", "")
    markers = _INST_DOMAIN_MARKERS.get(ctype)
    if markers is None:
        return True
    h = (domain or "").lower()
    marker_ok = any(m in h for m in markers)
    return _area_match(domain, target) or marker_ok
```

改 `discover_one` 调用处(221 行):
```python
    inst_ok = True if known else _institution_match(resolved, target)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m pytest tests/l1_collect/test_institution_area.py tests/l1_collect/test_channel_discovery.py -q`
Expected: PASS(新 5 测试 + 旧 discovery 测试;若旧测试调 `_institution_match(domain, "商务")` 需同步改传 dict)

- [ ] **Step 5: Commit**

```bash
git add scripts/l1_collect/channel_discovery.py tests/l1_collect/test_institution_area.py
git commit -m "feat(l1): 核验门 marker→host行政区(省/市拼音段)+marker双信号·消除真站误判"
```

---

## Task 5: discovery 候选 → 池

**Files:**
- Modify: `scripts/_oneshot/expand_channels_l1.py`(候选 → 池)
- Modify: `scripts/l1_collect/review_pool.py`(加 `candidate_entry` 辅助,便于测试)
- Test: `tests/l1_collect/test_review_pool.py`(追加)

- [ ] **Step 1: 追加失败测试**

```python
# tests/l1_collect/test_review_pool.py 追加
from scripts.l1_collect.channel_catalog import Channel, ChannelStatus


def test_candidate_entry_shape():
    from scripts.l1_collect import review_pool as rp
    ch = Channel(city="浙江省商务厅", province="浙江省", level="省", city_code="330000",
                 channel_type="商务", root_domain="x.zj.gov.cn", list_url="http://x.zj.gov.cn/l",
                 source="discovery", status=ChannelStatus.候选)
    e = rp.candidate_entry(ch)
    assert e["kind"] == "checkpoint" and e["ref"] == "x.zj.gov.cn"
    assert e["suggested_action"] == "promote" and "商务" in e["evidence"]
```

- [ ] **Step 2: 跑确认失败**

Run: `python3 -m pytest tests/l1_collect/test_review_pool.py::test_candidate_entry_shape -q`
Expected: FAIL(`candidate_entry` 不存在)

- [ ] **Step 3: review_pool.py 加辅助**

```python
def candidate_entry(ch) -> dict:
    """discovery 候选 Channel → 池条目(kind=checkpoint)。"""
    return {"kind": "checkpoint", "ref": ch.root_domain,
            "reason": "discovery_candidate_unverified", "suggested_action": "promote",
            "confidence": None, "evidence": f"{ch.channel_type} {ch.city} {ch.list_url}"[:80],
            "channel": ch.city, "run_label": "discovery"}
```

- [ ] **Step 4: expand_channels_l1.py 接池**

在 `main()` 的 `save_catalog(...)` 之前,把候选写池:
```python
    from scripts.l1_collect import review_pool
    for c in discovered:
        if c.status == ChannelStatus.候选:
            try:
                review_pool.append(review_pool.candidate_entry(c))
            except Exception as e:
                print(f"  [pool-write 失败] checkpoint/{c.root_domain}: {str(e)[:80]}")
```

- [ ] **Step 5: 跑确认通过**

Run: `python3 -m pytest tests/l1_collect/test_review_pool.py -q`
Expected: PASS(5 passed)

- [ ] **Step 6: Commit**

```bash
git add scripts/l1_collect/review_pool.py scripts/_oneshot/expand_channels_l1.py tests/l1_collect/test_review_pool.py
git commit -m "feat(l1): discovery 候选→review_pool(kind=checkpoint)"
```

---

## Task 6: sweep 命中 → 池

**Files:**
- Modify: `scripts/_oneshot/sweep_existing_commentary.py`(命中 → 池)
- Test: `tests/l1_collect/test_sweep.py`(追加)

- [ ] **Step 1: 追加失败测试**

```python
# tests/l1_collect/test_sweep.py 追加
def test_sweep_writes_hits_to_pool(tmp_path, monkeypatch):
    import scripts._oneshot.sweep_existing_commentary as sw
    from scripts.l1_collect import review_pool as rp
    pool = tmp_path / "pool.jsonl"
    hit = tmp_path / "【某政策解读】-x.md"
    hit.write_text("---\ntitle: 某政策解读\n---\n正文", encoding="utf-8")
    entries = sw.pool_entries([hit])
    for e in entries:
        rp.append(e, pool_path=pool)
    rows = rp.load(pool_path=pool)
    assert len(rows) == 1 and rows[0]["kind"] == "sweep"
    assert rows[0]["suggested_action"] == "confirm"
```

- [ ] **Step 2: 跑确认失败**

Run: `python3 -m pytest tests/l1_collect/test_sweep.py::test_sweep_writes_hits_to_pool -q`
Expected: FAIL(`pool_entries` 不存在)

- [ ] **Step 3: sweep_existing_commentary.py 加 pool_entries + main 接池**

```python
def pool_entries(hits: list) -> list:
    return [{"kind": "sweep", "ref": p.name,
             "reason": "title_commentary_marker", "suggested_action": "confirm",
             "confidence": None, "evidence": p.name[:80],
             "channel": "sweep", "run_label": "sweep"} for p in hits]
```
在 `main()` 里 `detect_commentary` 之后、`route_files` 之前加:
```python
    from scripts.l1_collect import review_pool
    for e in pool_entries(hits):
        review_pool.append(e)
```

- [ ] **Step 4: 跑确认通过**

Run: `python3 -m pytest tests/l1_collect/test_sweep.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scripts/_oneshot/sweep_existing_commentary.py tests/l1_collect/test_sweep.py
git commit -m "feat(l1): sweep 命中→review_pool(kind=sweep)·APPLY仍作过渡drain"
```

---

## Task 7: 收尾 pool summary

**Files:**
- Modify: `scripts/l1_collect/run_incremental.py`(`run_incremental` 收尾打印 summary)

- [ ] **Step 1: 改 run_incremental summary**

在 `print(f"[run_incremental] DONE {summary}")` 之前加:
```python
    pool_summary = review_pool.summarize()
    if pool_summary:
        print(f"[run_incremental] review_pool: {pool_summary}")
```

- [ ] **Step 2: 跑回归**

Run: `python3 -m pytest tests/l1_collect/test_run_incremental.py -q`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add scripts/l1_collect/run_incremental.py
git commit -m "feat(l1): run_incremental 收尾打印 review_pool summary"
```

---

## Task 8: 八步采集法 SOP 重写为纯 L1

**Files:**
- Modify: `策略-八步采集法.md`(先定位:`git -C "$VAULT" ls-files | grep 八步` 或 pipeline `docs/`)

- [ ] **Step 1: 定位文件**

Run: `cd ~/dev/政策分析-pipeline-l1-repair && (ls docs/*八步* 2>/dev/null; VAULT="$HOME/Documents/Zayn Main/政策分析"; git -C "$VAULT" ls-files | grep 八步)`
Expected: 输出文件路径(此前 Task12-5 已贴"已过时"header)

- [ ] **Step 2: 重写**

把"加工逻辑"段(原 Step4.5 抽取/5C business_view schema/6.5/7/8 的归属/分析)从 SOP 剥离,替换为一行指针 `（加工逻辑见 L2:scripts/l2_*，本 SOP 只覆盖采集 L1）`;保留纯采集步(发现→扫→抓→gate→入 raw)。移除 Task12-5 贴的"已过时"header(因已重生)。

- [ ] **Step 3: 人工核 + Commit**

```bash
git add <八步文件路径>
git commit -m "docs(l1): 八步采集法重写为纯L1 SOP(加工逻辑剥离指向L2)"
```
(vault 文件则 `git -C "$VAULT" add/commit`)

---

## Task 9: docs/BACKLOG.md 追加 + 核验门标 ✅

**Files:**
- Modify: `docs/BACKLOG.md`

- [ ] **Step 1: 追加 B15 + 核验门状态**

在 BACKLOG.md 末尾(登记说明行之前)追加:
```markdown
## B15 · L1 人工池 ↔ B14 接口

**是什么**:L1 已落 4 个源质量池的进池 IN(gate/checkpoint/sweep/fetch_fail → `state/l1_review/pool.jsonl`)+ OUT 契约(每 kind 裁决枚举见 `review_pool.VERDICTS`)。**消费者(回灌 applier)+ 人交互面 = B14**(独立 session)。过渡期回灌用现有 oneshots(promote_checkpoint / sweep APPLY)。

**捡起时**:B14 协议落地后,L1 加回灌消费者(按 `{ref,kind,verdict,...}` 消费,各 kind apply 见 spec OUT 契约表)。

**核验门标记法 backlog ✅ 已修(2026-06-08)**:`_institution_match` 改 host 行政区(省/市拼音段)+ marker 双信号,消除 CHECKPOINT 手工提升依赖。

**深度反爬 推后**:`swt.hebei` 类反爬站浅层(重试+入池)已做;UA轮换/代理/headless 归服务线。
```

- [ ] **Step 2: Commit**

```bash
git add docs/BACKLOG.md
git commit -m "docs(l1): BACKLOG 加 B15(L1池↔B14接口)+核验门标✅+深度反爬推后"
```

---

## 最终验收

- [ ] 全测试绿:`python3 -m pytest tests/l1_collect/ -q`(预期 105 旧 + 新增全过)
- [ ] 干跑无网验证池:构造一个 mock llm 低置信项跑 `_gate_extracted_dir`,确认 `state/l1_review/pool.jsonl` 落条目
- [ ] 派最终 code-reviewer 审整体(尤其:pool 写入 try/except 不中断渠道;review/ 不被清;核验门无 per-domain 特例;§C 池在 state/ 非 raw)
- [ ] 用 superpowers:finishing-a-development-branch 收口

---

## Self-Review(plan 对 spec)

- **覆盖**:组件 A(Task1+2+3+5+6+7 进池)/B 核验门(Task4)/C 抓取(Task3)/D 文档(Task8+9)+ OUT 契约(Task1 VERDICTS + Task9 doc)= spec 全覆盖。
- **占位符**:无 TBD;每步含真实测试/实现代码 + 确切命令。
- **类型一致**:`review_pool.append/load/summarize/candidate_entry/POOL/VERDICTS` 全 task 一致;`_gate_extracted_dir` 4 元组返回贯穿 Task2/7;`_institution_match(domain, target)` 新签名贯穿 Task4。
- **已知需同步**:Task2 改 `_gate_extracted_dir` 返回 4 元组 → 旧 `test_run_incremental.py` 若断言 3 元组需改;Task4 改 `_institution_match` 签名 → 旧 `test_channel_discovery.py` 若直调需改传 dict。两处在对应 Step4 跑回归时暴露并修。
