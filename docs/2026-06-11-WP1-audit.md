# WP-1 审计结论（Claude，2026-06-11）

> 对象：WP-1a 死信告警/sweep 代码（report `2026-06-11-codex-wp1a-report.md`）+ WP-1b 部署与 424 backfill（report `2026-06-11-codex-wp1b-report.md`）。
> 审计方法：不采信 Codex 自述——独立复跑测试、逐 diff 核对、服务器实态直读、数字闭合性核算。

## 1. 过关审计：通过

**WP-1a（代码）**：
- diff 范围 6 文件全在契约内，零越界（别条线未跟踪文件未碰）。
- 独立复跑 `python3 -m pytest`：**576 passed**（前基线 566 + 新增 10）。
- cron 行与设计逐字一致；cap 截断/归档 rotate/放弃上限/notify 语义均有测试。
- 已 ff 合 main 推送（`bb52a6c`）。

**WP-1b（部署+backfill）**，服务器实态独立直读全部吻合：
- `last_sync_run.json`：synced **1176**（基线 792，+384）/ relation **1123**（基线 992，**+131 悬挂边恢复**，与 WP-0 预测一致）/ errors=[] / skipped_invalid 15 / commentary 171。
- 队列 0、无死信文件、review_queue 0；vault 干净 HEAD==origin/main（`9dd43e2`）。
- bv 文件 1192；428 条清单 5 批 drain 全程 **failed=0**；投毒告警链路验证 `notified:true` 且用户飞书实收。
- 报告 commit `4571422` 仅含报告文件本身。

## 2. 阶段评估

- 覆盖闭环主体达成：raw(含 id) 1224 中 1176 已投影（97%）。**剩余尾巴 29 条 pid "ok 但无 business_view"**——机制：drain→run_2b apply 无 accepted 草稿则不写任何文件、StageResult 仍 ok（设计内：OPERATIONS Step 5C "异步、可缺失"）。不是静默失败，也不在人工池（queue=0）。
- 29 条肉眼分类：**~23 条为噪声类**（新闻/工作动态/征求意见稿/绩效公示/监督抽查/招投标——judge 拒绝归属属正确行为，其中"可开放容量/EPC 招标"两条实为 market_intel 形态，佐证 B1 第三源边界）；**~6 条疑似真政策被拒**（P_2025_NDRC_910 零碳园区、P_2025_NEA_53_a 新型电力系统试点、P_2025_HI_FGW_899 海南需求响应、P_2024_HA_15a936f0 源网荷储、P_2025_SH_61 碳达峰工作安排、P_2025_NDRC_efca57ff 抽水蓄能办法）→ **挂入 L2 质量抽样审计**（OPERATIONS §7 既有任务），不在闭环工程内逐条修。
- 偏差记录：428≠424（WP-0 后每日 cron 又产生增量，口径预期内）；synced 1176 vs bv 1192−invalid 15 = 1177 有 1 条差值（疑似 dup-pid wart 边缘案例，S3 既记）；WP-1a TDD 红绿挤单 commit（已在 WP-2a 红线强制改正）。

## 3. 洞察

1. **"ok 但无产物"是观测盲区**：run_l2 的 ok 计数掩盖"judge 拒绝归属"与"成功写入"的区别。建议（小改,排 WP-6 前）：run_2b/orchestrate 给 StageResult 加 `wrote_bv: bool`，drain 汇总打 `ok_no_draft` 计数——闭环审计巡检即可监控该数突增。
2. **today 实战体检副产品**：告警链路三处暗伤（入站模型 gpt-5.5、哨兵 feed 口径误判、venv 缺 Pillow）全部当日修复/排队，可见闭环的价值已经在第一天兑现。
3. relation 1123 vs canonical 1138：仍有 15 边悬挂（端点=skipped_invalid 的 15 条），与数字闭合。WP-2 以 **1123** 为投影基线、**1138** 为 canonical 基线。

## 结论：WP-1 关闭，WP-2 放行。
