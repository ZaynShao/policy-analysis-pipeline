---
title: 政策分析项目踩坑沉淀(LESSONS)
purpose: 记录项目建设期已经踩过、已经验证、已经付出代价的原则
audience: 任何接手本项目或重启同类项目的人 / AI
writing_rule: 只写原则+理由+处置,不引任何具体文件路径、脚本名、commit hash
status: v1.0 草稿(2026-05-08)
---

# 项目踩坑沉淀

> 这份文档是建设期已经付出代价换来的判断。不是"未来要遵守的规则",而是"已经被违反过、并且付出过清理代价"的红线。
>
> 每条结构:**规则 / 理由 / 触发场景 / 处置**。
>
> 不引具体路径、脚本名、字段名,以保证文档跨实现可用。具体落地见各 repo 内 SCHEMA / OPERATIONS。

---

## A. 数据层基本原则(必须先读)

### A1 · Raw 不可变(Immutable)

**规则**:原始数据一旦入库,任何后续流程禁止修改其内容(frontmatter 业务字段 + body)。

**理由**:Raw 是 trust 的锚点。一旦允许"派生发现 raw 不准就改 raw",事实层和解读层就混了,所有下游派生都失去基线。

**触发场景**:派生时发现 raw 描述不准 / 渲染缺字段 / 评分迭代要更新 / 反链回写 / LLM 总结想替代原文。

**处置**:重抓重入,旧版归档到 `_archive/` 类隔离区,新版用版本后缀区分。**不就地覆盖,不就地编辑**。

### A2 · 派生分层

**规则**:任何对 raw 的解读、评分、关系判断、跨数据对比、LLM 生成内容(摘要/标签/分析)—— 全部属派生层。按字段性质 split 到对应位置:通用客观描述 / 关系图 / 业务私有判断,**三处分离**。

**理由**:不分层会导致两件坏事:(1) 公开数据混入业务敏感判断,无法对外开放;(2) 重抽派生时业务字段被一并覆盖。

**触发场景**:LLM 抽取时想顺手把摘要、业务标签、影响分析都打到一处。

**处置**:抽取前明确每个字段的"性质",落到对应文件。LLM prompt 里明确写"只输出客观元数据,不做业务判断"。

### A3 · 关键判定:LLM 生成 = 派生

**规则**:**任何**经 LLM 生成或推断的内容都是派生,即便看起来"客观"(如摘要、规范化标题)。

**理由**:"客观"是主观判断。第一次违反就是把 LLM 摘要/标签当"客观字段"灌进 raw,后续不得不写多个清洗脚本批量回滚。

**触发场景**:正则抽取后觉得"还差一个标题精简,顺手让 LLM 兼掉"。

**处置**:正则/确定性提取归入元数据抽取阶段,所有 LLM 调用统一隔离到独立的派生阶段。两阶段必须有明确边界。

### A4 · 边界例外要白名单化

**规则**:Raw 不可变有合法例外(如 vault 内部关系网链接 / 评论反向匹配回填),但例外字段必须显式列入白名单文档,不允许"约定俗成"。

**理由**:边界一旦含糊,会被逐步扩张:今天加一个 `related_policy`,明天加一个 `business_tag`,半年后 raw frontmatter 里又长出业务字段。

**触发场景**:某个流程"只是回写一个链接,应该问题不大"。

**处置**:每次新增白名单字段都走 schema 修改流程,有评审记录。判定标准——指向已有数据的链接型字段或枚举型分类标签可以,LLM 生成的自由文本不行。

### A5 · 派生必须可追溯 + 可重现

**规则**:每个派生产物必须能指回 raw 源(`pid` 或等价标识);LLM 调用必须记录 prompt + temperature + 模型版本 + 时间。

**理由**:无法追溯的派生 = 失去 trust 锚点 = 等同幻觉。无法重现的派生在 schema 升级时无法判断"这次跑出来不一样是 prompt 改了还是模型变了"。

**触发场景**:为了快速看一眼结果,LLM 一把跑完就把日志关了。

**处置**:派生脚本必须自带写日志能力,日志不可选。日志里包含 input hash + prompt hash + model + 时间 + 输出。

---

## B. 采集流程踩坑

### B1 · 元数据抽取阶段不做语义判断

**规则**:抽取阶段只做确定性的客观字段(文号、日期、机构、URL、标题字面)。任何业务标签、语义分类、摘要生成、重要性评分,**都不在抽取阶段**。

**理由**:语义判断会随业务理解迭代,客观字段不会。混在一起意味着每次评分体系调整都要重跑全量抽取。

**触发场景**:抽取 prompt 里"反正都调 LLM 了,顺便让它判断业务关联吧"。

**处置**:抽取 prompt 里**显式列禁止字段**,模型如果输出禁止字段就视为抽取失败重试。

### B2 · 查重必须三维(URL + 文号 + 标题)

**规则**:查重不能只用标题哈希,必须 URL + 文号 + 标题三维联合,优先级 URL > 文号 > 标题。

**理由**:第一次只用标题查重,实测约 20% 政策是同 URL 重复入库——不同采集时点抓回的 PDF 版 vs HTML 版被当成两条新政策。

**触发场景**:候选政策"标题略有不同就当新的"。

**处置**:URL 入库前去 query/fragment/末尾斜杠归一化作 fingerprint;文号去机构前缀作归一化;标题去括号/空格/标点哈希。任一维度命中即为已入库。

### B3 · 抓取必须有兜底链路

**规则**:主抓工具(商业 API)必须配兜底路径(开源工具 + 通用爬取)。兜底路径要在 SOP 里明确,不是"出问题再说"。

**理由**:商业 API 配额会耗尽、海外可用性有波动,实测兜底路径在某些类目反而成功率更高。等额度耗尽再临时找替代,会丢一批数据或拖到下轮。

**触发场景**:"这次配额够用,先这样"。

**处置**:兜底链路从一开始就实测打通,配额监控触发降级。降级路径产物 schema 与主路径一致(以便后续合并)。

### B4 · 评论范式:Push 主 / Pull 兜底

**规则**:对持续涌入的内容(如评论、解读),走订阅 push 范式(本地缓存 + 异步消费),而不是逐条主动 pull。

**理由**:第一版用 pull(每条政策入库后主动搜评论),反爬风险高、历史覆盖不全、效率低。改成 push 后,反爬归零、历史一次性可见、本地存全文,且评论池越大反推渠道盲区越准。

**触发场景**:"先 pull 着用,以后再换"。

**处置**:能 push 的内容源不要 pull。对 push 不可达的源(私有/小号),才走 pull 兜底,且降级到人工。

### B5 · 评论关联是后置流程,不是抓取时同步建立

**规则**:Push 范式下,评论先大规模入库,关联关系是**后置异步**做(多路精度递进:严格匹配 → 模糊匹配 → LLM 精判)。

**理由**:抓取时强行建立关联会卡住抓取链路,且单条匹配精度低、上下文不够。后置匹配能拿到全 vault 上下文,精度反而高。

**触发场景**:"既然在抓评论,顺便把它跟政策连上吧"。

**处置**:抓取阶段只入库不匹配。匹配作为独立 step,按"高置信→中置信→低召回 LLM"递进,每路置信度不同字段记录。

### B6 · 模糊匹配精度未校验前不进 raw

**规则**:任何基于相似度阈值的批量回写(如标题模糊匹配反链),必须先抽样校验精度,精度报告归档,再决定是否入库。

**理由**:第一次模糊匹配做了一批回填,文档写"精度估计 70-85%,需抽样校验",**实际没校验就用上了**。后续不确定哪些链接是真的、哪些是误连。

**触发场景**:"算法跑出来了,先回填,以后再校验"。

**处置**:模糊匹配 → 抽样 → 校验报告 → 阈值固化。校验未通过前,产物只写"候选",不写正式关联字段。

---

## C. 工程组织踩坑

### C1 · 工程层与数据层物理分离

**规则**:数据(raw + 派生产物)和工程(脚本 + 中间状态 + 文档)应该物理分仓,以接口契约(schema)解耦。

**理由**:同 repo 时,每次工程迭代都会在数据 repo 里堆 staging / oneshot / audit / tmp,数据 repo 越来越脏。半年后想清理,无法判断哪些中间产物还在用。

**触发场景**:"小项目放一起方便,以后再拆"。

**处置**:从第一天起就分仓。Schema 文档作为唯一契约,工程仓只通过 schema 读写数据仓。

### C2 · Staging 不进数据仓

**规则**:抓取的暂存区(staging)、批次工作目录、试跑输出,都属工程中间产物,不进数据仓。

**理由**:第一次混在一起,半年后数据仓里有 11 个不同时期的 staging 目录,数百 MB,无人敢删——不知道哪些已入库哪些没。

**触发场景**:"暂时放这,跑完就移"。

**处置**:Staging 路径在脚本里硬隔离到工程仓 state 目录。入库脚本完成后,staging **必须由脚本自动归档或清理**,不依赖人工记得。

### C3 · 一次性脚本(oneshot)跑完即归档

**规则**:数据迁移、字段清洗、批量修复这类一次性脚本,跑完立即移到 archive 目录,不与正式管道脚本同目录。

**理由**:第一次没区分,正式 scripts 目录里堆了 27 个 oneshot,新人无法判断哪些是当前管道必跑。

**触发场景**:"先放 scripts/ 下,反正暂时还要跑一两次"。

**处置**:oneshot 命名前缀 + 独立目录。跑完打完日志立即 git mv 到 archive。如果发现某个 oneshot 跑了多次,说明它其实不是 oneshot,要么提到正式管道,要么修复 root cause。

### C4 · 中间产物分目录,不堆根

**规则**:派生输入、派生输出、audit 报告、状态快照,**各有分目录**,不允许堆在根目录。

**理由**:第一次没分,工程仓根目录散落 30+ 个 JSON / JSONL 文件,主题输入、冲突候选、diff 候选混在一起,新人无从下手。

**触发场景**:"一个文件而已,先扔根上"。

**处置**:目录骨架在 repo 创建时就立(derive_inputs / derive_outputs / audit / state),不允许根目录有散文件。

### C5 · 当前状态由脚本生成,不手写

**规则**:README / 主文档里写的"当前数字"(政策数、评论数、关系边数、覆盖率)必须由脚本生成,人工不直接编辑这些数字。

**理由**:第一次手写,README 写 289 政策、CLAUDE 写 263 政策,实际 1020。不一致会让任何接手的人对项目判断失真。

**触发场景**:"这次就手改一下"。

**处置**:状态自动生成脚本是基建之一(每周或每次大动作后跑),输出独立 STATUS 文档。所有引用"当前数字"的地方链接到 STATUS,不复制数字。

### C6 · SOP 改动要带 changelog

**规则**:核心流程文档(SOP / Pipeline / Schema)的每次修改要附 changelog 条目,而不是 inline 覆盖原文。

**理由**:第一次没做,SOP 经过多轮"边干边改",同一份文档里"原 SOP / 解耦版 / 重写版"混杂,读者无法判断哪些段还有效。

**触发场景**:"改一下原文就行,反正 git 有历史"。

**处置**:文档头部维护 changelog 表(日期 + 改了什么 + 为什么)。重大改动用"新版替换旧版"+ 旧版归档,而不是同一段反复编辑。

### C7 · /tmp 路径里的脚本不算"已实现"

**规则**:任何被 SOP 文档引用的脚本,必须在工程仓内有正式归档路径。`/tmp/` 或临时目录里的脚本视为"实验",不能作为流程依赖。

**理由**:第一次 SOP 里写"工具:/tmp/xxx.py",机器重启脚本就丢了。后续重跑必须重新写。

**触发场景**:"先放 /tmp 跑通,跑通了再归档"。

**处置**:脚本只要进入 SOP 引用,就必须先归档到工程仓,不允许 SOP 引用临时路径。

---

## D. AI 协作踩坑(本项目特有)

### D1 · 文档不引具体路径,只写原则

**规则**:跨阶段、跨项目可能流转的文档(LESSONS / ARCHITECTURE / SCHEMA),不引具体路径、脚本名、commit hash。

**理由**:AI 接手新阶段时,看到老路径就会去读老代码,然后开始模仿老结构——哪怕老结构正是这次想推翻的。

**触发场景**:"这条原则的来源是 X 脚本,写一下指过去方便溯源"。

**处置**:溯源放 git log / changelog,不进可流转文档。文档只表达"做什么 / 不做什么 / 为什么"。

### D2 · Schema 是契约,不是从代码反推

**规则**:数据 schema 由独立文档定义,作为读写双方的契约。AI 实现新版 pipeline 时不允许 grep 老脚本来"理解 schema 长什么样"。

**理由**:从老代码反推 schema 会把老代码的 bug 和约定一起继承。Schema 必须是被显式声明的,不是从行为推断的。

**触发场景**:"先看看老脚本怎么读这个字段,我照着写"。

**处置**:Schema 文档维护成"对外 API 文档"的级别。新代码只看 schema,需要参考行为时通过运行老代码看 input/output,而不是读老代码 source。

### D3 · 反污染靠物理隔离,不靠"禁止读"

**规则**:防止 AI 参考已废弃代码,最有效的手段是把它放到当前工作目录之外,而不是在 CLAUDE.md 里写"禁止读"。

**理由**:AI 对"禁止读"的服从度有限(尤其默认宽搜的 subagent)。但"不在 cwd 范围内"的目录,AI 不会主动去找。

**触发场景**:"老代码留在 _archive 里就行,我让 AI 别读"。

**处置**:废弃产物直接迁出 cwd,放到完全独立的归档仓库或独立目录。CLAUDE.md 的"禁止读"是冗余保险,不是主防御。

### D4 · LLM 派生产物归一通过同一脚本入库

**规则**:无论是 subagent 跑出来的还是主 session 跑出来的,LLM 派生产物入库走同一个 deterministic 脚本(读输入 → 校验 → upsert)。

**理由**:不同入口入库会导致格式微差(字段顺序、键名 case、空值表示),后续 lint 或 diff 时会扯不清。

**触发场景**:"subagent 跑完直接把 jsonl 提交了"。

**处置**:任何 LLM 输出落库前必须过格式校验脚本。Subagent 只产出原始 jsonl,主 session 跑入库脚本完成 upsert。

### D5 · Subagent 只跑独立任务

**规则**:Subagent 适合跑边界清晰、无副作用、无需共享状态的任务(如批量 LLM 抽取一批文档)。**不适合**跑需要回写多处、需要事务一致性的任务(如更新 raw + 派生 + 反链)。

**理由**:Subagent 是隔离沙箱,不能直接读 vault / 写共享文件。强行让它跑回写,要么数据不一致,要么需要复杂的"提交回主 session 再合并"机制。

**触发场景**:"反正都是批量任务,丢给 subagent 并行跑快"。

**处置**:任务拆成"产数据(可并行 subagent)+ 入库(主 session 串行)"两层。Subagent 不直接修改持久化层。

---

## E. 跨层判断(架构级反思)

### E1 · 数据模型的稳定性 > 工具链的先进性

**规则**:数据 schema 一旦稳定,工具链可以随便换;反过来,工具链稳定了 schema 还在变,意味着没真正想清楚。

**理由**:本项目 schema 经过 3 个版本迭代才稳定(v1 minimal → v2 nested → v3 split),但工具链(Tavily/Firecrawl/trafilatura)从一开始就稳。意味着前期精力应该花在 schema 设计上,不是工具调参上。

**触发场景**:"工具调通了再说 schema"。

**处置**:Schema 设计放在工具实测之前,先想清楚"未来 5 年这个字段会变吗"。会变的字段从一开始就放派生层,不放 raw。

### E2 · "边界例外"是糟糕设计的信号

**规则**:如果一份文档需要反复解释"X 原则有一个例外是 Y",大概率是 X 原则没设计对,不是 Y 真的特殊。

**理由**:本项目 "Raw immutable + 但允许写 related_policy 等关系字段" 这个例外解释了三处,说明 raw 的边界没真正稳定。如果 vault 关系网本来就该作为独立的"关系层"而不是"raw 边界例外",原则会更干净。

**触发场景**:"这条原则在某些情况下是例外,我们补充说明一下"。

**处置**:出现例外时,先反思原则是否切错了。如果切对了再写例外,且例外字段强制白名单 + 评审。

### E3 · 重抽成本决定派生粒度

**规则**:派生层的设计粒度,要跟"完整重抽一次的成本"匹配。重抽便宜的可以颗粒粗,重抽贵的(LLM 调用)必须颗粒细 + 增量。

**理由**:本项目 business_view 是 LLM 派生,956 篇全量重跑成本不低。早期没考虑增量,schema 微调一次就要全量重跑。后来虽然加了增量逻辑,但是补的。

**触发场景**:"先全量跑通,后面优化"。

**处置**:LLM 派生从第一天就设计 input hash + 缓存 + 增量。不依赖"重跑成本能接受"。

### E4 · 服务对象决定派生形态

**规则**:派生层的产物形态(jsonl / yaml / md / markdown 反链页 / 渲染产物)由**最终服务对象**决定,不是由"什么方便"决定。

**理由**:本项目同时服务 (1) 决策层简报阅读 (2) Obsidian 内部 graph 浏览 (3) 自动化 lint / 渲染。三种用途对派生形态需求不一致,如果只想着"jsonl 方便",graph view 就废了。

**触发场景**:"派生先存 jsonl,以后再考虑展示"。

**处置**:每个派生层产物在设计时明确"主要消费者是谁",对应主要消费者选格式。次要消费者通过 derive-on-the-fly 适配。

### E5 · 长文档要拆,不要长

**规则**:超过 500 行的核心文档基本无人完整读完。超过 800 行的文档,AI 也只读开头和结尾。文档过长本身就是设计气味。

**理由**:本项目核心 SOP 845 行,实际任何接手的人(包括 AI)都从来没完整读过。等于这后半部分内容只是"存档",起不到指引作用。

**触发场景**:"这块逻辑复杂,文档详细一点没关系"。

**处置**:核心文档控制在 300 行以内。详细推理、历史决策、特殊场景拆出独立文档,主文档只放原则 + 当前流程。

---

## 附录 · 这份文档的使用方法

- **不要把这份文档当 SOP 读**——它是"避坑清单",不是"操作手册"。SOP 由各 repo 内 OPERATIONS 文档管。
- **新阶段开始前先过一遍**——重启项目、换 AI 接手、引入新协作者时,这份文档应该是必读 onboarding。
- **新坑要追加,不修改老条目**——老条目不删除,新坑追加在对应章节。每条独立成立,不依赖上下文。
- **每条都是真实代价换的**——不要因为"现在看起来不太可能犯"就删除。建设期会忘记当时为什么这么痛。
- **路径出现立即清洗**——任何接手者发现这份文档里出现了具体路径/脚本名/commit hash,要把它替换成抽象表达。这是反污染纪律。

---

_v1.0 · 2026-05-08_
